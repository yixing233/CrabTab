# 更新日志 (Changelog)

所有面向用户的公开发布记录、新功能介绍与体验优化均记录在此文档中。

---

## [v1.3.0] - 2026-09-18

### ✨ 新增功能
- **主屏中央统一为「关闭 / 快捷方式 / 最近访问」三态开关**：
  - 原先「主屏展示内容」与「快捷方式显示」是两个互相影响的设置，可以组合出「选了快捷方式、却又把显示关掉」这类自相矛盾的状态，主屏空白时也看不出原因；现在合并为一个开关，三选一，语义唯一；
  - 开关常驻在主屏中央的工具条上，三种状态下都随手可切。此前切到「最近访问」或关闭后，工具条连同切换入口一起消失，只能绕回设置面板才能切回来；
  - 升级后原有选择自动保留（原本看最近访问的仍是最近访问，原本关闭的仍是关闭）；原「简洁」模式已并入「快捷方式」，窄屏下同样自动排成 4 列，窄屏观感不变。

### 🎨 优化
- **快捷方式与最近访问共用同一块主屏舞台**：两个状态的内容区左右边界都对齐搜索框，切换时不再整体错位——此前快捷方式固定 680px 宽、最近访问可宽至 1280px，切换瞬间边界跳变近 600px；工具条位置与内容起始高度也已统一，卡片流与图标网格从同一条线开始。
- **最近访问卡片材质统一**：改用与快捷方式图标、搜索框一致的毛玻璃质感，并移除按网址自动分配的六种底色——此前同一站点的不同页面会呈现不同颜色（如绿、黄、蓝），看着像按类别着色，实际与内容无关；悬停反馈改由描边增亮承担，质感更沉稳。
- **垂直位置调节量程统一**：时间的垂直偏移，滑块与紧邻的数字输入框现在量程一致（此前滑块只能拖到 ±60，输入框却可填到 ±100，同一行内自相矛盾）；搜索框与最近访问的偏移量程在浮层与设置面板中也已统一，不再出现「在浮层里拖不动、要进设置面板才能设到位」的差异。

### 🐞 修复
- 修复「跟随系统」主题下、系统为深色时，快捷方式区域的工具条、图标与翻页按钮仍按浅色渲染，与页面其余部分明暗割裂的问题。
- 修复浅色模式下「时钟外观」设置面板文字与白色底色重合、几乎无法辨认的问题。
- 修复矮屏或高缩放下，倒数日顶栏标题被挤成逐字竖排、并与下方胶囊左右错位的问题，同时提升其文字在浅色壁纸上的清晰度。
- 修复调大「全局界面缩放」时快捷方式图标反而变小的问题——此前放大所触发的矮屏收缩规则会把放大效果抵消掉。

---

## [v1.2.0] - 2026-09-17

### ✨ 新增功能
- **全局界面缩放系统（适配任意显示器与 Windows 系统缩放）**：
  - 在「设置 → 外观」新增「全局界面缩放」调节模块，提供「紧凑 (85%)」、「标准 (100% - 默认)」、「舒适 (110%)」、「大屏 (120%)」、「超大 (130%)」快捷档位与 80% ~ 140% 自由连续滑块；
  - 无论不同物理显示器尺寸，还是 Windows 常见的 100% / 125% / 150% 系统缩放，都能把界面调到最贴合自身双眼的舒适大小；
  - 首屏无感注入，打开新标签页瞬间即为设定比例，不会先闪一下默认尺寸。
- **快捷方式工具栏（添加入口常驻 + 一站式快捷设置）**：
  - 移除原先悬停在网格边缘才隐现的圆形「+」浮动按钮，改为网格上方常驻的独立工具栏，「添加快捷站点」以虚线胶囊常驻右侧，一眼可见、随时可点；
  - 工具栏左侧新增「快捷方式设置」下拉菜单，常用调节不必再层层深入设置面板，内置**显示模式切换**（关闭 / 简洁 / 桌面）、**自动补位开关**、**紧凑重整图标**与**桌面分页管理**（新建分页、删除当前分页、清理多余空白页），并实时显示分页占用概览；
  - 菜单与设置面板双向联动，改动即时生效并自动保存。
- **搜索偏好与位置一体化下拉浮层**：
  - 搜索框内部右侧收敛为一个极简箭头按钮，替代以往分散占位的多个按钮，大幅释放横向输入宽度；
  - 点击即可集中控制**搜索书签匹配**、**搜索历史匹配**，以及 **-120px ~ +120px 搜索框垂直位置**微调，并提供「紧凑靠上 / 标准居中 / 优雅下沉」快捷档位与一键重置。

### 🎨 优化
- **简洁模式「快捷设置」融入磁贴**：
  - 简洁模式不再单独占用一行工具栏，设置入口改为与站点图标同规格的「快捷设置」磁贴，紧接「添加快捷站点」排列在图标流末尾，视觉与网格完全统一；
  - 点击磁贴弹出的精简菜单只保留简洁模式用得上的**显示模式切换**，桌面专属项自动隐藏，避免无效干扰；
  - 菜单以磁贴为锚点智能定位，右对齐并在空间不足时自动翻转，任何缩放比例下都不会越出屏幕或遮挡搜索框。
- **缩放全档位精准适配**：重构底层视口计量方式，彻底解决放大后内容溢出被裁切的问题——右上角操作栏、搜索框居中位置、快捷方式网格、右下角倒数日抽屉在 80% ~ 140% 全档位下均完整可见且严格居中；笔记本矮视口下的紧凑排版改为依据真实可用高度判定，不再出现底部内容被裁掉且无法滚动到的情况。
- **右键菜单与浮层零偏移**：修复缩放状态下右键菜单偏离鼠标位置的问题，菜单贴边时自动向内翻转，永不超出屏幕。
- **倒数日高对比度自适应配色**：引入 WCAG 2.1 AA 级对比度适配，浅色/明亮毛玻璃背景下把容易发虚的亮黄、浅琥珀自动调优为沉稳的深琥珀金与深赤橙，对比度提升至 4.5:1 以上；暗色模式下继续保持灵动明亮，全场景清晰易辨。

### 🐞 修复
- **升级 / 重新加载扩展不再抹除用户数据**：
  - 修复部分场景下扩展更新或重新加载后，用户设置被重置为默认值的问题，语言、主题、壁纸、搜索引擎、缩放比例、字号样式、桌面分页等全部配置项原样保留；
  - 修复清空全部快捷方式、待办事项或倒数日后，刷新页面又被旧数据或默认列表「复活」的问题，删除操作现在会被如实记住；
  - 扩展升级瞬间若存储短暂不可用，界面会临时使用默认外观并自动暂停写入，绝不用默认值覆盖真实数据，待存储恢复后自动重新载入用户的真实配置；
  - 本地缓存损坏或格式非法时不再受影响，自动改用云端同步的完好数据并保证页面正常打开。

---

## [v1.1.2] - 2026-09-16

### 🌦️ 天气高精度逆地理引擎与全域乡镇支持
- **高精度逆地理编码深度下沉（覆盖全国 2800+ 区县及乡镇街道）**：
  - 接入全自动动态逆地理编码引擎，告别传统基于有限省会中心城市的粗粒度几何距离反查，彻底解决非省会及周边县市（如福建长汀县等）被机械匹配至外省省会（如南昌市）的痛点；
  - 深度检索第 7~8 级行政区划，优先呈现精准的乡镇街道名称（如“大同”、“乌镇”、“中关村”等），无乡镇时平滑呈现精准区县名（如“长汀”、“昆山”、“义乌”）；
  - 全面支持全球海外城市经纬度动态识别与当地气象服务；
  - 内置 2.5 秒极速熔断机制与就近节点保底，断网或网络抖动时 0 延迟平滑降级，绝不卡死新标签页。
- **自定义固定城市持久记忆**：
  - 支持通过持久化配置覆盖固定常驻城市，无论如何刷新均稳定保持设定位置。

### ✨ 界面视觉与细节进化
- **书签文件夹实心化设计升级（Solid Fill）**：
  - 顶部常驻书签栏各主目录文件夹、多级层叠下拉菜单子文件夹、右侧「其他书签」及主页书签中心目录胶囊卡片全面升级为**实心琥珀黄（Solid Amber）**填充；
  - 消除空心线框的虚浮感，视觉分量感扎实饱满，更贴合经典主流浏览器原生体验。
- **Bing 搜索引擎图标光学尺寸重构**：
  - 剔除 `bing.svg` 冗余的 15 组底边干扰路径与无效留白，紧缩 viewBox 画布边缘（体积直降 70%）；
  - 赋予 `scale-[1.25]` 光学补偿，使细长单字母与 Google、百度、DuckDuckGo 等饱满圆形/方块达到视觉体量完全平衡。
- **精简内置预设快捷方式**：
  - 将初次安装预置的 18 个站点精简为 8 个顶流核心站点（GitHub、Bilibili、YouTube、DeepSeek、ChatGPT、知乎、V2EX、Notion），首屏更加清爽通透。

---

## [v1.1.1] - 2026-09-16

### 🚀 性能与缓存革命
- **书签与全站站点图标本地多级持久化缓存（0ms 秒级直出）**：
  - **内存 + IndexedDB 双级缓存引擎**：构建极速 Favicon 缓存体系，打开新标签页时首发直接读内存缓存，图标 0ms 瞬间点亮，彻底告别重复网络探测与闪烁；
  - **Chrome 原生本地数据库优先级跃升**：在扩展环境下将 Chrome 原生本地已缓存图标（`_favicon/`）直接提至顶级首推候选，免联网、0 延迟、0 错误率；
  - **失败探测智能记忆**：针对确定无可用 Favicon 的冷门站点，持久化降级为精致的双色首字字母徽标，避免每次打开新标签页都发起十几个无意义的 404 远程网络探测；
  - **同域名智能继承**：支持同域名多级路径自动复用主站已验证的可用图标，极大减少并发请求。

### ✨ 交互与视觉升级
- **常驻新版本更新提示组件（告别 5 秒即逝的 Toast）**：
  - 淘汰原先仅弹出 5 秒后自动消失的轻量 Toast 提示，新增常驻型毛玻璃新版本通知卡片（`UpdateNotification.tsx`）；
  - 检测到新版本时在顶栏右下方常驻悬浮，清晰展现新版本号、更新摘要、「查看更新」与「Release 发布页」直达按钮，点击一键导航至设置关于面板；
  - 顶栏设置齿轮按钮同步点亮高亮脉冲徽标；即使用户点击 `✕` 忽略浮动卡片，设置图标仍保留常驻角标提醒；
  - 接入本地缓存快速读取机制，打开新标签页 0ms 瞬间挂载，无需等待网络检测。
- **右下角置顶倒数日支持往右侧平滑收起与展开**：
  - 倒数日胶囊堆叠区顶部新增精致微控制条，点击「收起 >」即可平滑向右滑出屏幕收拢；
  - 收起后自动转化为右侧贴边浮动微胶囊（`rounded-l-full` 毛玻璃微结构），实时保留最紧急倒数日图标、标题与剩余天数；点击即刻从右侧平滑展出完整列表；
  - 自动持久化记忆收起/展开偏好（`localStorage`），重开新标签页依然保持所选状态。
- **顶部常驻书签栏大屏幕空间释放与宽屏自适应**：
  - 彻底移除原先对顶部书签栏的过小定宽限制，在保证左侧天气组件与右侧控制栏呼吸间距的前提下，智能自适应填满横向可用视口，让 2K / 4K 及宽屏显示器能舒适展示更多常用书签。
- **搜索框快捷开关焕新（紧凑图标 + 右上角圆形复选框）**：
  - 搜索框右侧的书签与历史开关升级为精致的方圆图标，并在右上角配备圆形复选打勾徽标；
  - 大幅释放搜索输入框的横向可用空间，检索范围开启/关闭状态直观醒目。
- **最近访问组件原生垂直位置微调**：
  - 在卡片流标题栏右侧新增专属的「↕️ 垂直调节」快捷按钮，点击即可呼出毛玻璃微调面板；
  - 支持 `-120px ~ +120px` 连续滑块与快速档位（靠上 / 居中 / 靠下 / 恢复默认），配合平滑硬件加速动画，随心对齐壁纸。
- **卡片点击响应优化**：
  - 移除了卡片区域的鼠标拖拽滑动手势，彻底消除误触，点击卡片毫秒级极速打开目标网页。

### 🎨 细节与显示修复
- **首张卡片左侧边距与对齐优化**：
  - 为横向滚动容器增加呼吸内边距，修复了第一张卡片左侧圆角与阴影被边缘裁切的问题，并与上方标题胶囊实现像素级对齐。
- **IP 地址与局域网站点智能识别**：
  - 完善对 `127.0.0.1`、局域网 IP 与 `localhost` 服务的站点解析，修复品牌名称被误截断为数字 `0` 的问题；
  - 优化内网站点图标探测，对无图标站点优雅回退为精致的高质感彩色首字徽标。
- **搜索联想卡片质感统一**：
  - 统一搜索下拉联想卡片与搜索框的背景透明度、毛玻璃模糊与边框风格，视觉浑然一体。

---

## [v1.1.0] - 2026-09-15

### ✨ 新增功能
- **常驻顶部书签栏（Top Bookmark Bar）与快捷显隐开关**：
  - 顶栏中央新增常驻书签栏，支持多级文件夹层叠悬浮展开与平滑滚轮横向滑动；
  - 顶栏右侧操作区新增「书签栏显示/隐藏」一键快捷开关，便捷直达与全屏沉浸视野随心切换；
  - 自动同步浏览器原生书签与官方高清站点图标，支持一键在当前页或新标签页打开。
- **主屏「快捷方式 / 最近访问」卡片式双模式**：
  - 主屏内容展示全面升级，可在「自定义快捷方式」与「最近访问记录流」之间一键切换；
  - **精美双层微盒卡片**：智能萃取站点官方品牌标识（如 Google、GitHub、知乎、Bilibili 等）与 Favicon，并匹配 6 组柔和低饱和马卡龙主题边框；
  - **喜爱网址一键置顶（Pin）**：支持将高频访问的网站钉在最前排，随用随走；
  - **高效轻盈的滚轮滑动**：优化鼠标滚轮横向翻页阻尼，一次滑动即可轻松翻阅卡片。
- **搜索框接入「书签与历史」全局即时联想**：
  - 键入搜索词时，毫秒级并发检索书签与历史访问记录，智能去重，支持全键盘（`↑` / `↓` 导航高亮，`Enter` 回车直达）；
  - 搜索结果紧凑流式呈现，辅以清晰的绿底 `书签`、紫底 `历史` 专属徽标。

### 🎨 视觉与体验优化
- **搜索卡片毛玻璃质感一体化**：
  - 统一搜索下拉联想卡片与搜索框的背景透明度、毛玻璃模糊与边框风格，浑然一体。
- **小屏幕与矮视口（笔记本/高缩放）自适应**：
  - 针对 1366×768、1440×900 笔记本屏幕及 125%/150% 系统缩放场景深度调优，动态适配时钟与网格间距，确保核心内容单屏舒适容纳无纵向滚动条；
  - 优化桌面分页微胶囊的内边距，翻页指示更紧凑轻盈；
  - 增强各类复杂壁纸背景下快捷方式文本阴影与字体对比度，字字清晰。
- **右下角桌面倒数日微胶囊精修**：
  - 规整倒数日卡片几何比例，优化胶囊内左侧圆形图标与外层圆角的同心对齐与内边距；
  - 精简设置界面中重复冗余的说明文字，排版更纯粹。
- **时钟字体选项本地化**：
  - 时钟个性化设置中的字体选项全面中文化（等宽代码、现代无衬线、优雅衬线、圆润柔和、灵动艺术体），选择更直观。
- **已收藏壁纸选中视觉调优**：
  - 收藏壁纸库中当前生效壁纸的高亮效果改为内描边贴合，避免破坏原图外边框质感。

### 🐞 问题修复
- **时钟垂直偏移调节异常**：
  - 修复了在时钟设置面板中拖动「垂直位置偏移」滑块时，偶发导致界面卡顿甚至页面异常重绘的问题；
  - 重构了偏移定位逻辑，确保在不同对齐模式下平滑移动且不影响下方搜索框的排版定位。

---

## [v1.0.5] - 2026-09-14

### ✨ 新增功能
- **新增「倒数日 / 纪念日」模块**：
  - 支持多分类事项管理与“按年/按月/按周”周期重复推算；
  - 内置智能系统级动态预设，包含「年度进度」、「周末倒计时」及法定节假日；
  - 全站移除 Emoji，全面采用统一、清爽的极简 Lucide 图标集。
- **桌面右下角伴随胶囊**：
  - 支持将多个倒数日独立置顶至主屏右下角，纵向贴边整齐堆叠，点击直达抽屉管理；
  - 采用统一定宽（`240px`）与两端对齐排版，搭配等宽数字与硬件加速毛玻璃微质感，彻底消除间距不均与边缘锯齿。

### 🎨 视觉与体验优化
- **时钟字体与字重全面升级**：
  - 深度重构字体族谱与字重渲染逻辑，Mono、Serif、Round、Art 等全风格现已完整支持「纤细极简」细体档位；
  - 精确注入 `fontWeight: 200` 极细轴，呈现通透精致的发丝级线条感。
- **界面沉稳化纯净体验**：
  - 移除时钟鼠标悬停放大动效，桌面视觉中枢更加沉稳纯粹；
  - 倒数日胶囊移除突兀的左侧彩条与缩放交互，改用通透的微光高亮边框。
