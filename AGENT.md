# Agent 规范与指导文档 (AGENT.md)

本项目开发与维护过程中需遵循以下设计与实现规范：

## 1. 图标规范 (Icons Standard)
- **严禁使用原生 Emoji**（如 🚀, ⚡, ⚠, ✓ 等字符）。不同操作系统、浏览器和深浅色模式对 Emoji 的渲染色彩、基线高度和大小差异极大，极易破坏 UI 的对齐与精致感。
- **统一使用 `lucide-react` 矢量图标**：
  - 测速极速/闪电：`<Zap size={...} />`
  - 高速网络/火箭：`<Rocket size={...} />`
  - 良好/完成：`<Check size={...} />` 或 `<CheckCircle2 size={...} />`
  - 告警/较慢：`<AlertTriangle size={...} />`
  - 更多业务图标均需从 `lucide-react` 选取。
- 图标配合 `shrink-0` 及对应语义颜色类名（如 `text-blue-500`、`text-emerald-500` 等），保持跨平台一致的矢量渲染品质。

## 2. UI 与布局规范
- 页面与弹窗/抽屉组件需严格适配暗色（Dark）和亮色（Light）模式。
- 图表组件与坐标轴：
  - SVG 图表内的文字严禁直接使用 `<text>` 配合 `preserveAspectRatio="none"`，防止在非等比自适应时字体拉伸变形或锯齿发糊。
  - 坐标轴刻度、标签文字应使用原生 HTML 绝对定位层渲染，保证高清抗锯齿与等宽字体（`font-mono`）展示。

## 3. 测速与动态曲线设计规范
- **时间轴锚定机制**：测速实时折线图的横轴（X 轴）应基于当前测试阶段的固定标准总时长（如下载 3.8s，上传 2.2s）作为映射基准，而非随已采集点的末尾动态伸缩。
- **逐点生长效果**：
  - 阶段开始时初始化起始点 `(t=0, speed=0)`；
  - 随着时间流逝，采样点平滑从左往右推进绘制，终点光标（呼吸发光圆点）实时跟随最新点移动，避免出现“一开始就横跨整屏撑满一大块”的突兀跳变；
  - 阶段结束时完成整条时间轴闭合。
